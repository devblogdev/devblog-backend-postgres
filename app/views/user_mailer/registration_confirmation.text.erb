Hi <%= @user.first_name %>,

Thanks for registering at DevBlog!!! To confirm your email, follow the below url:

<% ENV["FRONT_END_URL_CUSTOM_DOMAIN"] || "localhost:8000" %>/registration-confirmation/<%= @user.confirm_token %>